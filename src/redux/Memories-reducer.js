

let initialState = {
    infoMemory: []
};

const memoriesReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}


export default memoriesReducer;


